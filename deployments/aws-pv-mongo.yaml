apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-mongo-aws
  namespace: mediaflow
  labels:
    type: volume
    tier: backend
spec:
  storageClassName: gp2
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/tmp/data"
