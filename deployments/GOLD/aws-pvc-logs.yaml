apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aws-pvc-logs
  namespace: mediaflow
  labels: 
    tier: backend
spec:
  storageClassName: backend-storage-class
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
