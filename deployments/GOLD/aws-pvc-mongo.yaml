apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aws-pvc-mongo
  namespace: mediaflow
  labels: 
    tier: backend
spec:
  storageClassName: gp2
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
